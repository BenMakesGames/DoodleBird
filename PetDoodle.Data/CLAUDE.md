IMPORTANT: This library contains serializable data models for the game. Keep dependencies to an absolute minimum - ideally ZERO third-party packages. Any dependency added here propagates to every consumer (game, tools, mods, save migrators, etc.) and risks coupling serialized save data to external types. If you think you need a dependency, reconsider: can the model be a plain POCO? Can the dependency live in a consumer project instead?
